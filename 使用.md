# 使用

## 平时只需要记住四件事

1. 需求和取舍跟聊天端说。
2. 正式执行前，让聊天端把要求写进任务文件。
3. 执行端只按任务文件做事，不自己找新任务。
4. 问“做完了吗”时，看实际改动、测试或其他可核对结果，不看自我评价。

## 新项目第一次使用

把项目目录交给聊天端，并明确说“初始化这个项目”。

聊天端会先读项目入口、依赖、运行与测试方式、主要代码入口和已有说明，然后建立项目自己的 `.ai/`。

不能确认的内容写“未知”，不猜。

## 创建任务

正式任务保存在：

```text
.ai/tasks/TASK-xxxx.md
```

任务至少写清：

```text
id
revision
status
role
base_branch
work_branch
goal
scope
forbidden
read_context
acceptance
report
```

这些字段是机器识别用的固定名字，字段内容用简体中文。

如果目标、范围、禁止事项、读取内容、验收条件或结果位置发生变化，先把 `revision` 加一，再让执行端继续。

## 聊天端给执行端的启动提示

本地项目示例：

```text
role: BUILDER
project: /本地/项目路径
task: .ai/tasks/TASK-0007.md
task_revision: 2
report: .ai/reports/TASK-0007-BUILDER.md
```

如果任务已经同步到 GitHub，再加一行精确提交：

```text
task_ref: <exact commit SHA>
```

启动提示只负责告诉执行端“去哪里读”，不重复整份任务内容。

## 执行端读取顺序

执行端只读：

1. 已安装的 `AGENTS.md`；
2. 当前角色规则；
3. 聊天端启动提示；
4. 当前 `TASK-xxxx.md`；
5. 任务明确要求读取的项目文件。

不要为了“可能有用”扫描全部历史。

## 完成以后

执行端把结果写到：

```text
.ai/reports/TASK-xxxx-<ROLE>.md
```

结果至少写：

- 做成了什么；
- 改了哪些文件；
- 做了什么检查，结果是什么；
- 还有什么没覆盖；
- 建议下一步做什么。

聊天端再读取实际改动和结果文件，决定接受、继续修改或做一次独立检查。

## 什么时候需要独立检查

- 低风险、范围清楚、证据充分：聊天端可以直接验收。
- 普通复杂或高风险：默认最多增加一个全新的独立检查角色。
- 只有真实故障、数据丢失、权限失效、状态损坏、无法解释的事实冲突、密钥泄漏或重复接管等事故，才扩大检查人数。

## GitHub 怎么用

GitHub 只用于同步和版本记录。

执行端如果收到远端项目地址：

1. 先同步到本地；
2. 在独立本地工作目录或分支执行；
3. 本地完成并验证；
4. 只有任务明确允许时，才把提交推回远端；
5. 不自行合并、不自行发布、不强制覆盖远端历史。

没有 GitHub 时，上述本地流程完全不变，只少了同步远端这一步。
