# 安装

这套规则分两处安装，业务项目本身不预装。

## 一、聊天端

打开 `聊天端/安装.md`，把 `CHAT_CORE.md` 加到聊天项目数据源，并让聊天项目固定按它工作。

## 二、执行端

打开 `执行端/安装.md`，把 `AGENTS.md` 和 `角色/` 复制到执行工具长期可读取的用户目录。

## 三、业务项目

什么都不要复制。

第一次使用时，把真实项目交给聊天端并明确说“初始化这个项目”。聊天端会根据真实代码和配置建立该项目自己的 `.ai/`。

## 四、远端仓库

可选。

使用 GitHub 时，把项目正常同步到远端；执行端开始前先同步到本地，完成后再按任务授权同步回远端。

不使用 GitHub 时，本地流程完全一样。
