# 检查清单

用于检查这套规则能否从空环境正常开始。

- [ ] 新聊天只安装 `聊天端/CHAT_CORE.md`，没有把执行端规则装进聊天项目。
- [ ] 新执行工具只安装 `执行端/`，没有把通用执行规则复制进业务项目。
- [ ] 一个全新的业务项目第一次接入前没有 `.ai/` 也能开始。
- [ ] 聊天端第一次接入后能创建 `.ai/INDEX.md`、四个项目记录文件、任务索引和当前状态文件。
- [ ] 聊天端能创建带 `revision` 的任务文件。
- [ ] 执行端只靠通用规则、角色规则、启动提示、任务文件和必要项目文件就能开始。
- [ ] 没有 GitHub 时，执行端仍能在本地完成任务并写结果文件。
- [ ] 有 GitHub 时，执行端先同步到本地，再本地执行，再按授权同步回远端。
- [ ] 聊天端能根据实际改动和结果证据验收，而不是只看执行端自评。
- [ ] 普通任务不会机械增加多名检查者。
- [ ] 换一个新聊天后，只读 `.ai/INDEX.md`、`.ai/tasks/ACTIVE.md`、`.ai/handoff/CURRENT.md` 和必要项目记录即可恢复。
- [ ] 主负责聊天切换时有人明确确认，不出现两个聊天同时当主负责人。
- [ ] 密码、令牌、私钥和其他密钥值不会进入 `.ai/`。
- [ ] 原项目出现新提交时，可以用 `维护/对应表.md` 做定向核对，而不需要复制原项目整套目录。
- [ ] 原项目的新规则若与本项目三条安装边界冲突，会先交给人决定，不会自动覆盖。
