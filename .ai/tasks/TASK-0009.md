# TASK-0009

id: TASK-0009
revision: 2
status: READY_TO_DISPATCH
role: BUILDER
base_branch: main
work_branch: build/task-0009-chat-architecture-repair
base_commit: 6f22088ed0bf544a5eaf19d92295cddccc9e3c06

goal: 将 D-011、D-012 与 `.ai/context/ARCHITECTURE.md` 中由 Chat 确定的架构实体化到 Chat-Git-Agent 产品文件，并落实此前 upstream audit 的核心修复：ARCHITECT-as-Chat、Durable Dispatch/role discoverability、minimal seed、Bootstrap durable evidence、cold-start release gate、live audit 语义、正式项目架构文档和 LICENSE 分发要求。

scope:
  - 先执行 Remote Sync Gate，以当前 live main 为基线建立隔离工作区/工作分支。
  - 将 `ARCHITECT` 从可派发 Agent 角色中退役：
    - 更新 `chat/CHAT_CORE.md`，把 Architecture Function 明确列为 Chat 内建职责，并规定 Chat 负责架构判断/内容、Builder 负责 `.ai/**` 外的文件实体化。
    - 更新 `agent/AGENTS.md`、README、USAGE、INSTALL 与其他产品引用，使 Agent 角色集合只保留 `RESEARCH / BUILDER / REPAIR / VERIFIER / RELEASE`。
    - 删除或彻底退役 `agent/roles/ARCHITECT.md`；最终安装说明和 release policy 不得再把它作为 Agent role 分发。
  - 创建正式项目文档：
    - `docs/ARCHITECTURE.md`
    - `docs/OPERATING_MODEL.md`
    - `docs/DISPATCH_PROTOCOL.md`
    内容必须忠实实体化 Chat 已接受架构基线，不自行重新定义角色模型。
  - 正式增加业务项目 task-specific Durable Dispatch 规范：
    - `.ai/dispatch/TASK-xxxx-<ROLE>.md` 作为标准位置；
    - dispatch artifact 包含 task/revision/role/project/startup_mode/report/common_rule/role_rule；Git/GitHub 模式再包含 exact task_ref/common_rule_ref/role_rule_ref/access；
    - exact dispatch pointer **不写入 dispatch 文件自身**，而由 minimal seed/外部派发记录指向 `<repo>@<commit>:.ai/dispatch/TASK-xxxx-<ROLE>.md`，遵守 D-012，避免 commit SHA 哈希自引用；
    - 业务项目不复制通用 `AGENTS.md` 或 `roles/*.md`；角色规则通过 exact/durable pointer 获取。
  - 将 Agent 启动接口改为 minimal seed 只寻址 exact durable dispatch pointer；不得在 seed 复制 TASK 正文、scope、acceptance、角色规则正文、同步步骤或模型/时间建议。
  - 强化 Bootstrap Check：开工七项检查结果必须形成 durable evidence；无证据不得进入实现/验证。
  - 更新 cold-start / audit 机制：
    - `maintenance/AUDIT.md` 不得继续把历史静态 PASS 当作 live PASS；
    - 增加明确 cold-start 验证入口（可使用 `maintenance/COLD_START.md` 或等价最小文件）；
    - release/upstream live PASS 必须引用一次 fresh Agent 从 minimal seed 成功恢复 exact dispatch → rules → exact TASK → bootstrap → durable evidence 的实际报告 pointer；仅检查“文档存在”不能 PASS。
  - 更新 `maintenance/UPSTREAM.md`，明确区分 `MUST_REPAIR` 与 `INTENTIONAL_DIVERGENCE`，不得声称 upstream full protocol parity。
  - 修复 LICENSE 分发规则：安装/发布包 policy 与 audit 必须要求分发包包含 `LICENSE`；现有 v1.1.0 ZIP/manifest 如缺 LICENSE，应明确标记为历史/不可作为新的 live release 证据。不要无用户版本决定擅自创建正式新版本/tag/release。
  - 更新 Release 包允许文件/manifest 规则：ARCHITECT role 文件不再允许；`LICENSE` 必须允许并要求包含。
  - 更新 README/USAGE 导航，使正式架构入口清楚，且说明 `.ai/context/ARCHITECTURE.md` 只是恢复快照。
  - 业务项目 `.ai` 标准增加 `dispatch/` 时，同步更新 Chat 规则、USAGE 和正式文档，保持单一语义。
  - 在 Agent 报告中给出实际修改文件、删除文件、验证结果、diff 摘要、exact work ref、剩余风险；不得把尚未执行的 fresh independent cold-start 验证描述为 PASS。

forbidden:
  - 不改变 D-001/D-002/D-005/D-006/D-007/D-008/D-009/D-011/D-012；发生冲突报告 `BLOCKED_CONTRACT_CONFLICT`。
  - 不重新引入独立 ARCHITECT Agent。
  - 不把 Chat 写权限扩大到业务项目 `.ai/**` 之外；Builder 只是按 Chat 架构实体化产品文件。
  - 不把通用规则或角色正文复制到业务项目 `.ai/`。
  - 不要求 dispatch artifact 写入包含自身的 commit SHA。
  - 不伪造 cold-start、Bootstrap、Git ref、测试或审计证据。
  - 不把历史 `maintenance/AUDIT.md` PASS 沿用为当前 live PASS。
  - 不覆盖/伪造 TASK-0006 缺失的原始 RELEASE Agent 报告。
  - 不自行决定新发布版本号，不创建 tag/GitHub Release，不 deploy/release。
  - 不 merge、不 force push、不重写历史、不删除无关远端 ref。

read_context:
  - `.ai/context/DECISIONS.md`（重点 D-011、D-012）
  - `.ai/context/ARCHITECTURE.md`
  - `.ai/reports/UPSTREAM-AUDIT-2026-08-22-CHAT.md`
  - `.ai/tasks/TASK-0008.md`（仅作为被取代历史，不能执行）
  - `.ai/handoff/CURRENT.md`
  - `README.md`
  - `INSTALL.md`
  - `USAGE.md`
  - `chat/CHAT_CORE.md`
  - `agent/AGENTS.md`
  - `agent/roles/*`
  - `maintenance/UPSTREAM.md`
  - `maintenance/AUDIT.md`
  - `maintenance/CHANGELOG.md`
  - `release/v1.1.0/Chat-Git-Agent-v1.1.0.manifest`
  - `release/v1.1.0/Chat-Git-Agent-v1.1.0.sha256`
  - `LICENSE`
  - upstream exact ref `youling/ai-use@1ddfeb0dd6a606dbc80ab86de8903d6888077bc6` 的 targeted interface/bootstrap/cold-start 文档

architecture_source:
  decisions:
    - `.ai/context/DECISIONS.md#D-011`
    - `.ai/context/DECISIONS.md#D-012`
  baseline: `.ai/context/ARCHITECTURE.md`

acceptance:
  - 产品中不再存在“ARCHITECT 是可派发 Agent”的有效接口；角色列表、安装说明、使用说明、Agent 通用规则和 release policy 一致。
  - `chat/CHAT_CORE.md` 明确 Architecture Function 属于 Chat，同时保持 Chat Write Guard：Chat 架构，Builder 实体化，Chat 最终验收。
  - 三个正式 `docs/**` 文件存在且职责清晰；其内容与 D-011/D-012/Chat 架构基线一致。
  - Durable Dispatch 的位置、字段、生命周期和 local/GitHub pointer 语义被正式定义；fresh Agent 不需要依赖人类记忆找到 role rule。
  - exact dispatch pointer 由 seed/外部派发记录提供，dispatch artifact 不自引用自身 commit SHA。
  - minimal seed 只寻址 dispatch；产品文档不再把长 `role/project/task/report/...` 块称作 canonical minimal seed。
  - Bootstrap Check 必须 durable 留痕；缺证据时不能继续执行/验证。
  - `maintenance/AUDIT.md` 明确 live PASS 需要 fresh cold-start 实证 pointer，且历史 PASS 不能复用。
  - LICENSE 被纳入分发包必需项；现有缺 LICENSE 的 v1.1.0 包不得继续作为 live release-ready 证据。
  - upstream compatibility 明确标注 intentional divergence，不宣称 full parity。
  - TASK-0006 缺失报告保持真实未收敛状态。
  - 完成本任务的文档/规则一致性检查，并报告 exact work ref；若无法做真正 fresh independent Agent 验证，明确留给后续 VERIFIER，不得假 PASS。

report: .ai/reports/TASK-0009-BUILDER.md

remote_actions:
  push_work_branch: allowed
  open_pr: forbidden
  merge: forbidden
  deploy: forbidden
  release: forbidden

user_authorized_actions:
  - push_work_branch
