# TASK-0005

id: TASK-0005
revision: 3
status: DISPATCHED
role: REPAIR
base_branch: main
base_commit: d1884200ad6ee8e1b3ad5f26fb6afc094d65ac3c
work_branch: repair/task-0005-task0003

goal: 在当前 main 控制面上承接 TASK-0003 已通过的产品改动，修复 TASK-0004 的真实阻塞，并完整补上 GitHub Remote Sync Gate 与 push / PR / merge / release 权限模型，产出新的可验收 v1.1.0 候选。

execution_base_policy:
  - `base_commit` 是本 revision 的计划基线，不代表 Agent 可以使用陈旧本地 checkout 直接开工。
  - GitHub 模式开工第一步必须刷新远端；Repair 分支必须从刷新后观察到的 live `origin/main` 建立。
  - live main 必须包含本 TASK revision 3 的 `task_ref`；若 task_ref 不可解析、live main 未包含当前任务、或远端漂移无法安全解释，停止并报告 `BLOCKED_REMOTE_DRIFT`。

permission_model_target:
  - GitHub/远端 Git 的正式 TASK 必须显式包含 `remote_actions`；未列出或值不明确的远端动作一律视为 `forbidden`。
  - `remote_actions` 至少覆盖：`push_work_branch`、`open_pr`、`merge`、`deploy`、`release`；需要创建 tag 或写默认分支时也必须有明确授权语义，不能从 `release: allowed` 或“有 push 权限”自行推导其他动作。
  - `push_work_branch: allowed` 只授权当前 TASK 指定的 work branch；不授权默认分支、其他协作者分支、force push、重写历史或删除远端 ref。
  - `open_pr: allowed` 只授权创建/更新当前 TASK 的 PR；不会自动授予 merge。
  - `merge` / `deploy` / `release` 对 `BUILDER`、`REPAIR`、`RESEARCH`、`VERIFIER`、`ARCHITECT` 永久视为角色不允许；若任务错误配置为 allowed，Agent 必须停止并报告权限冲突，不自行执行。
  - 只有 `RELEASE` 角色可以进入 merge/deploy/release 执行阶段，而且必须同时满足：TASK 对该动作显式 `allowed`、TASK 中有用户明确授权记录、Chat 已记录被操作的 `accepted_work_ref`/accepted release target、Remote Sync Gate 已通过、当前 live refs 与已验收对象一致。
  - TASK 中新增 `user_authorized_actions`（或等价明确字段）作为 durable 权限证据；只有 Chat 可根据用户当前明确指令写入。Agent 不得自行新增、扩大或推断用户授权。
  - merge 成功后必须回读 default branch exact ref；release 前必须以回读后的 live release target 再核对版本、tag、包/哈希和授权。merge 授权不会自动授予 release，release 授权也不会反向授权未允许的 merge。

scope:
  - 开工前执行 Remote Sync Gate：刷新远端 refs，记录 live default branch、live main、`task_ref`、Builder 产品 ref、Verifier ref 和本地 HEAD；不得使用未刷新的本地分支作为事实源。远端不可刷新时报告 `BLOCKED_REMOTE_SYNC`。
  - 从刷新后的 live `origin/main` 建立隔离修复分支；不得从旧 Builder base `fbcdc55...` 作为最终合并基线。
  - 将 Builder 已通过内容检查的产品改动 `9e07c42420b642bcd6f5609e7dc57618cc90f553` 承接到当前 main，只承接 TASK-0003 允许的产品/交付语义，不用旧 Builder 分支的 `.ai/**` 状态覆盖当前控制面。
  - 修正 `maintenance/AUDIT.md` 对上游 handoff transaction 的过度断言：只声明经证据支持的“交出记录 + 恢复核对 + 用户确认 + 接任确认 + 缺失即 BLOCKED”等作用对应语义；不得声称已经实现原项目特定命名的 `ARCHITECT_HANDOFF_REQUEST` / `ARCHITECT_HANDOFF_ACCEPTED` 事件端点、durable pointer 字段或当前并不存在的精确结构。
  - 在 `agent/AGENTS.md` 增加硬性 `Remote Sync Gate`：任何 GitHub/远端 Git 任务在 `fresh`、`resume`、`VERIFIER`、`REPAIR` 开工前必须先 fetch/refresh 远端并核对 live default branch、`task_ref`、任务 target/work refs 与本地状态；没有同步证据不得进入实现或验证。
  - 在 `agent/AGENTS.md` 增加完整 Remote Action Gate：Agent 在任何 push/open PR/merge/deploy/release 前必须读取 TASK 的 `remote_actions`；字段缺失即禁止；动作不在当前角色权限内即停止；禁止从工具能力、分支名、验收 PASS 或其他动作权限推导授权。
  - 在 `agent/AGENTS.md` 明确 push 流程：仅当 `push_work_branch: allowed` 时可 push 当前 work branch；push 前再次 refresh 远端并比较 remote work branch；存在未解释推进时停止；不得 force；push 后回读并报告远端 exact ref。
  - 在 `agent/AGENTS.md` 明确 merge/release 流程：非 RELEASE 角色禁止；RELEASE 必须同时检查 TASK `remote_actions`、`user_authorized_actions`、Chat 的 accepted exact ref、live refs 和保护规则；缺一即 BLOCKED。merge 后回读 main；release 前再核对 live target、版本/tag、asset manifest/hash；发布后回读 Release/tag/asset。
  - `Remote Sync Gate` 至少区分：同步失败 → `BLOCKED_REMOTE_SYNC`；task/ref 与远端发生无法安全解释的漂移 → `BLOCKED_REMOTE_DRIFT`；本地工作分支/远端工作分支发生并发变化 → 停止并报告，不 force、不猜。
  - `fresh`：刷新远端后再建立/选择工作分支；`resume`：刷新远端后先比较任务 revision、live main、work branch 和上次记录的 refs，再决定是否安全继续。
  - `VERIFIER`：必须先刷新 live main 和被验证的 exact ref/branch，再以远端可解析对象做验证；不得只看本地 checkout。报告必须记录 `remote_main_checked`、`task_ref_checked`、`target_ref_checked` 等可恢复证据。
  - `REPAIR`：必须先刷新远端和当前 main，再基于 live main 修复；不能拿旧失败候选直接当作新 main 基线。
  - 在 `chat/CHAT_CORE.md` 补充 GitHub 派发/验收的远端同步门与权限合同：Chat 创建 GitHub TASK 时必须显式写 `remote_actions`；未写则全部远端动作禁止。Chat 只有在用户明确授权 merge/deploy/release 时才能把对应动作写为 allowed，并在 TASK 中记录 `user_authorized_actions`；不能因为用户允许开发或 push 就推导 merge/release。
  - 在 `chat/CHAT_CORE.md` 明确验收后的权限推进：Chat 可记录 `ACCEPTED_WORK_REF`，但这本身只代表内容验收，不授予 merge/release。merge/release 必须新建或修订 `RELEASE` TASK，并携带 accepted ref、live target 和用户授权。
  - 更新 `agent/roles/RELEASE.md`：merge/deploy/release 只能在当前 RELEASE TASK 明确允许且有用户明确授权时执行；分别核对 accepted ref、live main/PR、merge 结果、release target、tag、asset hash；每个阶段写后回读。merge 与 release 是两项独立授权。
  - 更新 `agent/roles/BUILDER.md`、`agent/roles/REPAIR.md`、`agent/roles/VERIFIER.md`（必要时其他执行角色），明确可按 TASK 显式权限 push 自己的 work branch，但不得 merge/deploy/release；不得把自验证/独立验证 PASS 转化为这些权限。
  - 在 `USAGE.md` 说明 Remote Sync Gate、完整 `remote_actions` 字段、默认禁止规则、push/PR/merge/release 的阶段门，以及 `BLOCKED_REMOTE_SYNC` / `BLOCKED_REMOTE_DRIFT` / 权限冲突场景。
  - 在 `maintenance/AUDIT.md` 增加 `remote_sync_gate` 与 `remote_action_permissions` 审计项，验证“无字段即禁止”“非 RELEASE 不得 merge/release”“accepted ref 不等于 merge/release 授权”“merge/release 分离授权”。
  - 独立确认 Cursor 当前官方规则链接；若现有 URL 只是官方重定向且内容仍可核验，可保留并在审计中注明；只有当前官方入口已明确变化且现有链接不再合适时才更新 `INSTALL.md`。
  - 因 `agent/AGENTS.md`、`chat/CHAT_CORE.md`、`USAGE.md`、至少一个 role 文件属于 Release 允许文件且本 revision 必须修改，必须重新生成 `v1.1.0` ZIP、manifest、SHA256；ZIP 仍严格只包含允许的 11 个文件。
  - 运行 `git diff --check`、发布审计、ZIP manifest/完整性/SHA256 校验；报告中给出可复现结果。
  - 报告中只使用远端可解析 ref 作为最终 `exact_work_ref`；不要把仅存在于临时本地对象库、远端无法解析的 SHA 当作最终交付 ref。
  - 完成后在 push 前再次刷新远端：确认远端工作分支没有被别人推进；记录 push 前 live main。若发生无法安全解释的并发变化，停止并报告，不强推。
  - 将结果写入 `.ai/reports/TASK-0005-REPAIR.md` 并 push 指定修复分支。

forbidden:
  - 不修改 TASK-0003 已通过且与本次修复无关的产品语义。
  - 不修改或删除当前 main 已存在的 `.ai/context/*`、`.ai/tasks/*`、`.ai/handoff/*`、历史 reports；只新增本任务指定报告。
  - 不把 Verifier 关于“旧 base 分支树不含 main 后续 `.ai/**` 就等于正常 merge 会删除这些记录”的推论当作 Git 三方合并事实；修复目标是基于 live main 产出干净候选。
  - 不允许任何 Agent 用“本地看起来最新”替代 Remote Sync Gate。
  - 本 TASK 自身只允许 `push_work_branch`；Repair 不得 open PR、merge、deploy、release、force push、重写历史、移动 main、创建 release tag 或删除远端分支。
  - 不新增第二个 Verifier；Repair 返回后由 Chat 基于 live remote refs 做最终复验，除非出现真实 Incident。

read_context:
  - .ai/tasks/TASK-0003.md
  - .ai/tasks/TASK-0004.md
  - .ai/reports/TASK-0003-CHAT.md
  - .ai/reports/TASK-0004-VERIFIER.md@verify/task-0004-task0003
  - .ai/context/DECISIONS.md
  - maintenance/AUDIT.md@9e07c42420b642bcd6f5609e7dc57618cc90f553
  - chat/CHAT_CORE.md@9e07c42420b642bcd6f5609e7dc57618cc90f553
  - agent/AGENTS.md@9e07c42420b642bcd6f5609e7dc57618cc90f553
  - agent/roles/RELEASE.md
  - agent/roles/BUILDER.md
  - agent/roles/REPAIR.md
  - agent/roles/VERIFIER.md
  - USAGE.md@9e07c42420b642bcd6f5609e7dc57618cc90f553
  - INSTALL.md@9e07c42420b642bcd6f5609e7dc57618cc90f553
  - release/v1.1.0/*@9e07c42420b642bcd6f5609e7dc57618cc90f553
  - youling/ai-use@1ddfeb0dd6a606dbc80ab86de8903d6888077bc6 当前 handoff / Agent interface / Git 相关有效入口

exact_refs:
  planning_main: d1884200ad6ee8e1b3ad5f26fb6afc094d65ac3c
  builder_product_ref: 9e07c42420b642bcd6f5609e7dc57618cc90f553
  builder_branch_tip_observed: 345ae52ca170eb9cb98bca4bbe85b6c7fadf6b60
  verifier_report_ref: 1ac96ee80a26280ad847450cdd2d6a018e94d19b
  upstream_ref: 1ddfeb0dd6a606dbc80ab86de8903d6888077bc6

remote_actions:
  push_work_branch: allowed
  open_pr: forbidden
  merge: forbidden
  deploy: forbidden
  release: forbidden

user_authorized_actions: []

acceptance:
  - Agent 报告开头给出 Remote Sync Gate 证据：`live_main_at_start`、`task_ref_checked`、`builder_ref_checked`、`verifier_ref_checked`、`local_head_before_work`，且 refs 均可解析。
  - 修复分支以开工时刷新后的 live `origin/main` 为祖先，并且该 live main 包含 TASK-0005 revision 3 的 `task_ref`。
  - 相对开工时 live main 的 diff 只包含 TASK-0003 已批准产品改动、本 revision 必需的 Remote Sync Gate/权限模型文档与角色规则、`maintenance/AUDIT.md` 修正、重新生成的 `release/v1.1.0/*` 和 `.ai/reports/TASK-0005-REPAIR.md`。
  - 当前 main 的 TASK-0003/TASK-0004/TASK-0005、ACTIVE、CURRENT、D-005~D-009 等 `.ai/**` 控制记录完整保留。
  - `maintenance/AUDIT.md` 不再声称实现不存在的特定 handoff 事件端点/字段；对上游只做有证据的作用对应 PASS。
  - `agent/AGENTS.md` 明确 Remote Sync Gate；fresh/resume/Verifier/Repair 都不能在未刷新远端时工作。
  - `agent/AGENTS.md` 明确 Remote Action Gate：GitHub TASK 无 `remote_actions` 或动作字段缺失时默认 forbidden；push/open PR/merge/deploy/release 不能互相推导权限。
  - `chat/CHAT_CORE.md` 明确 GitHub TASK 必须显式远端权限；只有用户明确授权才能记录 merge/deploy/release allowed；`ACCEPTED_WORK_REF` 不自动授予 merge/release。
  - `agent/roles/RELEASE.md` 明确 merge 与 release 是独立授权，且只有 RELEASE 角色 + TASK allowed + `user_authorized_actions` + accepted/live refs 全部满足才可执行。
  - Builder/Repair/Verifier 等非 RELEASE 角色明确不得 merge/deploy/release；若 `push_work_branch` allowed，只能 push 指定 work branch，不能 force/默认分支/其他分支。
  - `USAGE.md` 给出清晰阶段：实现/验证 → push work branch（若允许）→ Chat acceptance → 用户 merge 授权 → RELEASE merge → main read-back → 用户 release 授权 → RELEASE release → release read-back。
  - `USAGE.md` 明确 `BLOCKED_REMOTE_SYNC` / `BLOCKED_REMOTE_DRIFT` 及权限冲突时停止执行。
  - Chat Write Guard、Dispatch Gate、平台特定 Agent 规则位置、无安装复制脚本等 TASK-0003 核心要求保持 PASS。
  - 新 ZIP manifest/完整性/SHA256 校验 PASS，并记录新的 SHA256；ZIP 文件集合仍严格为 11 个允许文件。
  - 最终报告给出远端可解析 `exact_work_ref`；push 后停止等待 Chat 最终验收，不 merge/release。

report: .ai/reports/TASK-0005-REPAIR.md

status_note: revision 3 已正式派发；revision 1/2 seed 全部作废。Repair 本轮仅允许 push 自己的工作分支，其他远端动作全部禁止。