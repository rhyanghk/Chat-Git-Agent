# TASK-0006

id: TASK-0006
revision: 1
status: DISPATCHED
role: RELEASE
base_branch: main
base_commit: ed4f8661e8c682d70f7f0cdb4976e3ac0cf704c1
work_branch: release/task-0006-merge-v1.1.0

goal: 将已验收的 v1.1.0 产品候选 `56815df11a80a8682f63c2fbfee9dd99af4dd3a6` 安全合并到当前 live `main`，保留当前 `.ai/**` 控制记录；完成 merge 后回读 default branch exact ref 并停止，不执行 release/deploy/tag。

accepted_work_ref: 56815df11a80a8682f63c2fbfee9dd99af4dd3a6
accepted_repair_branch_tip: 0e3a9e7f731c7f5a2e8f67f784e0c604249a0f11
accepted_zip_sha256: 613520456c70725b6b982aecfe970d320a530b7fa280983cef3ffda85bd585d8
accepted_by: .ai/reports/TASK-0005-CHAT.md

execution_base_policy:
  - `base_commit` 是创建本任务时的 planning main；Agent 开工前必须先执行 Remote Sync Gate，并以刷新后的 live `origin/main` 为实际 merge target。
  - live main 必须包含本 TASK revision 1 的 `task_ref` 和当前验收控制记录；若 task_ref 不可解析、live main 未包含该 revision、或发生无法安全解释的漂移，停止并报告 `BLOCKED_REMOTE_DRIFT`。
  - merge source 必须是精确 `accepted_work_ref`，不得改用旧 Builder ref、Repair 分支 tip 或其他未经 Chat 接受的产品 ref。

scope:
  - 开工前刷新远端 refs，记录 live default branch、live main、task_ref、accepted_work_ref、repair branch tip、local HEAD 和保护规则状态。
  - 确认 `accepted_work_ref` 可解析，且其产品内容与 `.ai/reports/TASK-0005-CHAT.md` 的接受记录一致。
  - 确认 `accepted_repair_branch_tip` 相对 `accepted_work_ref` 只增加 Repair 报告；Repair 报告已由 Chat 收敛进当前 main，因此 merge source 仍使用 `accepted_work_ref`。
  - 从刷新后的 live main 建立隔离 RELEASE 工作现场；执行正常 Git merge，保留 live main 的 `.ai/**` 控制记录与 accepted_work_ref 的产品/规则/release 文件。
  - 不 squash、不 rebase、不 cherry-pick 替代 merge、不 force、不重写历史。若正常 merge 出现冲突，停止并报告 `BLOCKED_MERGE_CONFLICT`，不得自行改变已验收产品语义或 `.ai/**` 控制事实来消冲突。
  - merge 结果在本地/隔离现场先检查：`git diff --check`，确认 current main 的 `.ai/**` 仍存在，确认 v1.1.0 产品文件和 `release/v1.1.0/*` 已进入结果树，确认 `.sha256` 仍记录 `613520456c70725b6b982aecfe970d320a530b7fa280983cef3ffda85bd585d8`。
  - 写远端前再次刷新 live main；如果 default branch 已被别人推进且无法安全重算 merge，停止并报告 `BLOCKED_REMOTE_DRIFT`；不得 force。
  - 仅在所有门槛通过后，把 merge 结果非 force 写入 `main`。该默认分支写入仅限本 TASK 的 merge 动作。
  - merge 写入后立即回读远端 `main` exact ref；验证新 main 同时包含 merge 前 live main 和 `accepted_work_ref` 的历史/内容，不得只相信本地成功消息。
  - 回读新 main 上的 `README.md`、`chat/CHAT_CORE.md`、`agent/AGENTS.md`、`maintenance/AUDIT.md` 和 `release/v1.1.0/Chat-Git-Agent-v1.1.0.sha256` 作为关键结果证据。
  - 把 merge 前 live main、accepted_work_ref、merge commit/new main ref、验证结果写入 `.ai/reports/TASK-0006-RELEASE.md`。
  - merge 成功并回读后停止。release、deploy、tag 均保持未授权；不得因为 merge 已成功而继续发布。

forbidden:
  - 不修改已验收产品内容来“顺手优化”或解决非冲突问题。
  - 不使用 `0e3a9e7...`、`9e07c424...` 或其他 ref 替代 `56815df...` 作为产品 merge source。
  - 不 force push、不重写历史、不删除分支/ref、不绕过保护规则。
  - 不 open PR；本任务授权的是已验收 ref 的 merge，不额外创建 PR 流程。
  - 不 deploy、不 release、不创建/移动 release tag、不上传 Release asset。
  - 不把 `user_authorized_actions: [merge]` 推导为 release/deploy/tag 授权。

read_context:
  - .ai/reports/TASK-0005-CHAT.md
  - .ai/reports/TASK-0005-REPAIR.md
  - .ai/context/DECISIONS.md
  - .ai/handoff/CURRENT.md
  - agent/roles/RELEASE.md@56815df11a80a8682f63c2fbfee9dd99af4dd3a6
  - agent/AGENTS.md@56815df11a80a8682f63c2fbfee9dd99af4dd3a6
  - maintenance/AUDIT.md@56815df11a80a8682f63c2fbfee9dd99af4dd3a6
  - release/v1.1.0/*@56815df11a80a8682f63c2fbfee9dd99af4dd3a6

exact_refs:
  planning_main: ed4f8661e8c682d70f7f0cdb4976e3ac0cf704c1
  accepted_work_ref: 56815df11a80a8682f63c2fbfee9dd99af4dd3a6
  accepted_repair_branch_tip: 0e3a9e7f731c7f5a2e8f67f784e0c604249a0f11

remote_actions:
  push_work_branch: allowed
  open_pr: forbidden
  merge: allowed
  write_default_branch: allowed_for_merge_only
  deploy: forbidden
  release: forbidden
  create_tag: forbidden

user_authorized_actions:
  - merge

acceptance:
  - Agent 开工报告 Remote Sync Gate 证据，live main 包含本 revision 的 task_ref。
  - merge source 精确为 `56815df11a80a8682f63c2fbfee9dd99af4dd3a6`。
  - merge 采用正常非 force Git merge；无 rebase/squash/cherry-pick/history rewrite。
  - merge 前 live main 的 `.ai/**` 控制记录完整保留，accepted_work_ref 的产品/规则/release 文件完整进入结果。
  - merge 后远端 main exact ref 已回读，并能证明同时包含 merge 前 live main 与 accepted_work_ref。
  - `release/v1.1.0/Chat-Git-Agent-v1.1.0.sha256` 仍为 `613520456c70725b6b982aecfe970d320a530b7fa280983cef3ffda85bd585d8`。
  - `git diff --check` 与必要结构检查 PASS。
  - 未执行 PR、deploy、release、tag、force push 或历史重写。
  - 报告写入 `.ai/reports/TASK-0006-RELEASE.md`，返回 merge 后远端 `main` exact ref 后停止。

report: .ai/reports/TASK-0006-RELEASE.md

status_note: 用户已于 2026-08-22 明确授权 merge；release/deploy/tag 未授权。TASK-0006 revision 1 已正式派发；Chat 进入 WAIT_RELEASE_AGENT_RESULT。