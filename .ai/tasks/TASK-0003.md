# TASK-0003

id: TASK-0003
revision: 1
status: DISPATCHED
role: BUILDER
base_branch: main
base_commit: fbcdc55fc6f019945a123b8468d38b4a12ef7151
work_branch: build/task-0003-dispatch-guard
goal: 落实 Chat 不进行开发的硬边界、完善 Agent 分发机制，并按用户要求更新安装说明、审计和最新资源包。
scope:
  - 在 `chat/CHAT_CORE.md` 增加明确的 Chat Write Guard：业务项目中 Chat 只允许写 `.ai/**`；任何 `.ai/**` 之外的产品/代码/配置/测试/项目文档修改必须形成正式 TASK 并派 Agent。
  - 在 `chat/CHAT_CORE.md` 增加 Dispatch Gate：TASK 写入并回读、取得 task_ref（使用 Git 时）、输出用户派发卡和最短 Agent 提示后，Chat 必须进入 `WAIT_AGENT_RESULT`，不得继续执行该 TASK 的开发内容。
  - 明确分发时机：事实不清派 `RESEARCH`；详细技术设计不清派 `ARCHITECT`；明确实现派 `BUILDER`；已确认故障派 `REPAIR`；独立验收派 `VERIFIER`；发布准备/执行派 `RELEASE`。
  - 扩展 `agent/roles/ARCHITECT.md`：负责深入技术设计、影响分析、接口/数据流/迁移方案和实现任务拆分；默认只输出报告，不直接实现业务代码，也不成为第二主协调 Chat。
  - 更新 `README.md` 与 `USAGE.md`，说明上述派发时机、分发流程、`WAIT_AGENT_RESULT` 和 Chat/Agent 职责边界。
  - 更新根目录 `INSTALL.md`。对于通过“把规则文件放到某个目录”完成安装的 Agent，只列出需要移动/放置的源文件和对应目标目录/文件名，不提供 shell、PowerShell、copy、cp、mv、ln、安装脚本或自动复制命令。
  - `INSTALL.md` 对主流 Chat 和 Agent 的安装入口只使用当前官方资料可核验的路径/界面；如果某产品只有 UI 级全局规则而没有官方固定文件目录，明确写 UI 操作，不虚构路径。
  - 保持 Chat 安装 / Agent 安装 / 业务项目零预装；GitHub 仍只是可选同步与版本工具。
  - 修改完成后对照 `youling/ai-use` 当前最新有效规则重新执行发布前审计；发现偏差先修正，审计 PASS 后才生成最新资源包。
  - 最新资源包只包含 `README.md`、`INSTALL.md`、`USAGE.md`、`chat/CHAT_CORE.md`、`agent/AGENTS.md` 和六个 `agent/roles/*.md`。
  - 建议资源包版本为 `v1.1.0`；生成 ZIP、manifest 和 SHA256，并在报告中记录。
forbidden:
  - Chat 不得直接修改本 TASK 的 `.ai/**` 之外文件；产品文件必须由 Builder Agent 在隔离工作区完成。
  - 不把 GitHub 设为运行前提。
  - 不把通用 `CHAT_CORE.md`、`AGENTS.md` 或 `roles/` 复制进业务项目。
  - 不为了“自动分发”引入新的数据库、Bot、Runner 审批链或第二任务合同。
  - 不把完整 TASK、长期记忆、验收清单或模型建议复制进 Agent seed。
  - 审计未 PASS 不得生成新资源包。
  - Agent 文件型安装不提供文件复制/移动脚本或命令。
read_context:
  - chat/CHAT_CORE.md
  - agent/AGENTS.md
  - agent/roles/ARCHITECT.md
  - README.md
  - INSTALL.md
  - USAGE.md
  - maintenance/UPSTREAM.md
  - maintenance/AUDIT.md
  - .ai/context/DECISIONS.md
  - youling/ai-use@1ddfeb0dd6a606dbc80ab86de8903d6888077bc6 当前有效入口
acceptance:
  - `chat/CHAT_CORE.md` 明确写出业务项目 Chat 只写 `.ai/**` 的白名单；越界必须 `BLOCKED_CHAT_WRITE_SCOPE` 并派 Agent。
  - 明确存在 Dispatch Gate 与 `WAIT_AGENT_RESULT`；完成派发后 Chat 不得继续该任务的设计实现/代码/配置/测试/产品文档修改。
  - 六个角色的分发时机可由 Chat 无歧义判断；ARCHITECT 与 BUILDER 职责明确分离。
  - `agent/roles/ARCHITECT.md` 覆盖深入技术设计但默认不实现业务代码。
  - `INSTALL.md` 中所有“文件放目录型 Agent”只写“源文件 → 目标路径”，没有文件移动/复制脚本或命令。
  - 主流 Chat/Agent 安装位置或 UI 入口具有当前官方依据；无法核验的写 unknown/unsupported，不猜。
  - 发布前 `maintenance/AUDIT.md` 重新审计并明确 `PASS`，包括与原项目无跑偏检查和 Chat 不开发检查。
  - 最新 ZIP 文件集合严格等于允许 manifest，且记录 SHA256。
  - Agent 报告包含实际 diff、审计结果、资源包路径/manifest/SHA256、剩余风险和 exact work ref。
report: .ai/reports/TASK-0003-BUILDER.md

supersedes: TASK-0002 的资源包/发布后续工作由本任务取代；TASK-0002 保留为历史证据。
status_note: 已完成正式派发；Chat 进入 WAIT_AGENT_RESULT，不再修改本任务的产品文件。
